<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">
<struts>
		
		

<!-- Struts 2运行时使用Spring来创建对象 -->
	<constant name="struts.objectFactory" value="spring" />
	<!-- 指定Web应用的默认编码集，相当于调用 HttpServletRequest的setCharacterEncoding方法 -->  
    <constant name="struts.i18n.encoding" value="UTF-8" /> 
    <!-- 请求去掉后缀 -->
     
    <constant name="struts.action.extension" value="action" />  


     <package name="allInterceptor" extends="struts-default" >
    	<interceptors>  
            <interceptor name="encode"  
                class="interceptor.EncodingInterceptor">  
            </interceptor>  
            <!-- 拦截器栈 -->  
            <interceptor-stack name="mydefault">  
                <interceptor-ref name="defaultStack" />  
                <interceptor-ref name="encode" />  
            </interceptor-stack>  
        </interceptors>  
    </package>
    
 <!-- struts2的action必须放在一个指定的包空间下定义 -->
    <package name="default" extends="struts-default" namespace="">
  
    	<action name="indexAct" class="app.action.ApplicationAction"  method="index">
    		<result name="success" type="dispatcher">/Market/views/application/main.jsp</result>
    		<result name="error" type="redirect">/Market/views/application/Login.jsp</result>
    	</action>
    	
    	
    	  <!-- 定义处理请求URL为login.action的Action -->
        <action name="login" class="app.action.memberLoginAction"  method="login"></action>
        <action name="logout" class="app.action.memberLoginAction"  method="logout"></action>
        <action name="checkLoginId" class="app.action.memberLoginAction"  method="checkLoginId"></action>
        <action name="regist" class="app.action.memberLoginAction"  method="regist"></action>
    </package>



</struts>